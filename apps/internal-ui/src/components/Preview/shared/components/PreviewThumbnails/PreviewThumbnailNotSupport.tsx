import { Icon } from '@/components/Icon';

const PreviewThumbnailNotSupport = () => {
  return <Icon className='text-in-gray-03 text-[1.875rem]' iconKey='image' />;
};

export default PreviewThumbnailNotSupport;
