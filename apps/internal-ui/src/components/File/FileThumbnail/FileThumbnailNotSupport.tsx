import { Icon } from '@/components/Icon';

const FileThumbnailNotSupport = () => {
  return <Icon className='text-in-gray-03 text-[1.5rem]' iconKey='image' />;
};

export default FileThumbnailNotSupport;
